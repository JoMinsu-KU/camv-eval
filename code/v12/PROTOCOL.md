# Bounded v12 review simulation, frozen before scientific execution

This is a post hoc reviewer-driven sensitivity analysis, separate from all earlier experiments and the four original empirical tests. No model calls or human labels are added. All outputs and caches stay on E: SATA HDD; previous scientific sources and results remain unchanged.

Three conditions are fixed: RF_H_C and RF_H_Q125 use REASSEMBLE-derived development/evaluation group-size and class-count profiles, continuous or floor-quantized scores (step 0.125), and continuous within-pair AUROC 0.58. The empirical development profiles comprise 111 groups, 1,716 trials and 121 failures; evaluation profiles comprise 37 groups, 577 trials and 50 failures in 21 groups. Each synthetic group independently draws one profile with replacement from its stage's empirical profile distribution. Counts match these totals in expectation; profiles and their distribution are treated as fixed DGM inputs, with no uncertainty claim for the profile distribution itself. Gaussian score effects are independent of profiles and labels conditional on the stated class shift. This checks class information and group sizes, not a fitted model of the empirical score distribution.

NE_H_C uses the prior Beta(2,3), three-trials-per-group law with 40 development and 120 evaluation groups, continuous within-pair AUROC 0.65, Joint offsets (-0.75,0,0.75), and Late offsets (0.75,0,-0.75). The score vectors are not exchangeable across method labels at fixed camera-pair labels. Their pair-minus-pooled *population-average* performance functionals have equal expectations by pair-permutation symmetry. Thus both procedure-average interactions equal zero exactly, while the estimated interaction is nondegenerate. This is one structured nonexchangeable-score null; it does not establish validity under every zero-interaction distribution. The preservation of symmetry after pair aggregation is explicitly acknowledged.

All laws retain overlapping-pair correlation from shared cameras, latent intragroup score correlation 0.35, method latent correlation 0.5, empirical recall target 0.90, inclusive decisions, and trial-weighted evaluation with equal pair weights. Gaussian marginal tail probabilities give exact fitted-policy truths. Class-profile sampling affects data sizes and threshold distributions, not the class-conditional marginal score oracle.

Use 1,000 outer datasets and 999 paired bootstrap draws per condition. Reuse the validated conditional percentile, refit percentile, refit basic, conditional sandwich-t, and two-sample cluster BCa code. Apply its existing invalid-draw rules without fallback. Two nonzero procedure-average targets use 16,384 independent development datasets per condition and report reference Monte Carlo SE. NE_H_C uses exact zero. Seeds use root (20260915,712031), independent domains 10/20/30/40/100, and scientific indices starting at zero; preflight indices 900001/900002 are excluded.

Report both fitted-policy and procedure-average coverage, planned/valid denominators, Wilson Monte Carlo intervals, mean widths, reference-target sensitivity, centered null rejection, and simulated class/group counts. At M=1000, the worst-case binomial Monte Carlo SE is 1.58 percentage points. Preserve all outer arrays for independent aggregation. No optional stopping, condition selection after results, or replacement of earlier confirmatory results.
